<%= form_with(model: [@list, @list.reviews.build], local: true) do |form| %>
  <%= form.text_area :comment, placeholder: 'Enter comment here', class: 'form-control' %>
  <%= form.submit 'Add Review', class: 'btn btn-primary mt-2' %>
<% end %>
